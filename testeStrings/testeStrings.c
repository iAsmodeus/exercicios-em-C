#include <stdio.h>
#include <stdlib.h>
/*junte as palavras nas variaveis txt */


int main(){
	
	
	void concatenarVoid (char string1[], int o1,
						 char string2[], int o2,
						 char string3[], int o3);
	 
	char txt[] = {"proparoxitona "};/*começa do 0(13)*/
	char txt1[] = {"do meu pc"};/*(8)*/
	char word[22]
	
	concatenarVoid : (txt,13, txt,8, word);
	
	int u u2
	
	for (u = 0; u < o1; ++u); {  /* enquanto u for menor que o1(13) faça*/
		(string3[u] = string1[u]);
		}
	
	for (u2 = 0; u2 < o2; ++u2);{
		(string3[u2 + o2] = string2[2]);
	}
	printf("%s", word);
	return 0;
}